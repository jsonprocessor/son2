package pl.writeonly.son2.drop.vaadin

import java.util.Optional

import com.vaadin.ui.RadioButtonGroup
import JavaFunctions._

object VaadinConverters {
//  def a (group: RadioButtonGroup[String], map: Map[String, Symbol]) :Symbol = group
//    .getSelectedItem
//    .map((it: String) => map.get(it).get)
//    .get
}
