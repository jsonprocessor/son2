package pl.writeonly.son2.core.providers
import pl.writeonly.son2.core.notation.{NotationReader, NotationWriter}

class ProviderImpl(in: NotationReader, out: NotationWriter)
  extends Provider(in, out) {
}
